{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The main window loads the daemon's web UI from loopback; grant it notification access so agent needs-input alerts reach Notification Center, and the opener so :cloud signin can send the account page to the system browser.",
  "windows": [
    "main"
  ],
  "remote": {
    "urls": [
      "http://127.0.0.1:8300"
    ]
  },
  "permissions": [
    "core:default",
    "notification:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://cloud.muxdeck.app/*"
        }
      ]
    }
  ]
}